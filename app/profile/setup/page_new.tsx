/*
 * Legacy stub: page_new.tsx
 * This file was a temporary working copy during profile setup refactor.
 * It is now deprecated and kept only to avoid accidental re-creation.
 * Safe to delete once confirmed no references remain.
 */

'use client';

export default function LegacyUnusedProfileSetupPage() {
  if (process.env.NODE_ENV === 'development') {
    console.warn('Legacy file app/profile/setup/page_new.tsx is unused and can be removed.');
  }
  return null;
}
